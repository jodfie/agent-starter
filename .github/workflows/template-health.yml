name: Template Health

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  baseline:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Make helper scripts executable
        run: chmod +x scripts/*.sh
      - name: Run baseline doctor
        run: ./scripts/doctor.sh
